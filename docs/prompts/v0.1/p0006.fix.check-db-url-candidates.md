Corrija um bug no supabase-backup-tool.

Contexto:
O backup.config.json está configurado corretamente com dbUrlEnvCandidates:

{
  "supabaseCli": "npx supabase",
  "defaultProject": "geral-homologacao",
  "backupRoot": "./backups",
  "projects": {
    "geral-homologacao": {
      "projectRef": "izzitgselrzkxsqglcja",
      "dbUrlEnvCandidates": [
        "GERAL_HOMOLOGACAO_DB_URL_POOLER",
        "GERAL_HOMOLOGACAO_DB_URL_DIRECT"
      ],
      "db": {
        "engine": "pgdump",
        "excludeTables": [
          "storage.buckets_vectors",
          "storage.vector_indexes"
        ]
      },
      "storage": {
        "mode": "rclone",
        "remote": "supabase-geral",
        "buckets": [
          "trip-receipts",
          "campaign-documents"
        ],
        "rcloneFlags": [
          "--s3-list-version",
          "2"
        ]
      }
    }
  }
}

Mas ao rodar:

npm run check -- --project geral-homologacao --test-db-connection

a ferramenta mostra:

[check] dbUrlEnv ausente para o projeto "geral-homologacao"

Causa provável:
src/checks.ts ainda valida obrigatoriamente project.dbUrlEnv, ignorando project.dbUrlEnvCandidates.

Correção obrigatória:
1. Ajustar src/checks.ts para aceitar dbUrlEnvCandidates como configuração válida.
2. Regras:
   - se project.dbUrlEnvCandidates existir e tiver pelo menos 1 item, usar essa lista;
   - senão, usar project.dbUrlEnv;
   - se ambos existirem, dbUrlEnvCandidates tem prioridade;
   - se nenhum existir, erro:
     [check] nenhuma URL de banco configurada. Configure dbUrlEnvCandidates ou dbUrlEnv.
3. Quando dbUrlEnvCandidates existir:
   - verificar presença das variáveis no process.env;
   - variável ausente deve gerar warning;
   - erro somente se nenhuma candidata estiver definida no ambiente.
4. Com --test-db-connection:
   - usar o mesmo seletor implementado em src/db-url-selector.ts;
   - testar as candidatas em ordem;
   - imprimir qual env foi selecionada;
   - manter senha mascarada.
5. Garantir que backup-db e checks.ts usam função utilitária comum para resolver a lista de candidatas, evitando divergência.
6. Atualizar README.md e .env.example se necessário.
7. Adicionar testes cobrindo:
   - projeto apenas com dbUrlEnvCandidates passa no check;
   - projeto apenas com dbUrlEnv continua passando;
   - projeto com os dois usa dbUrlEnvCandidates;
   - projeto sem ambos falha;
   - candidata ausente gera warning, mas não erro se outra candidata existir;
   - nenhuma candidata presente no process.env gera erro;
   - --test-db-connection usa src/db-url-selector.ts.
8. Manter:
   npm run typecheck
   npm run build
   npm run test